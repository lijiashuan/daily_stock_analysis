# 股票智能分析系统 - 全面技术分析报告

**生成时间**: 2026-05-23  
**项目名称**: daily_stock_analysis  
**版本**: 基于最新代码审查

---

## 📋 目录

1. [项目概述](#一项目概述)
2. [核心功能模块](#二核心功能模块)
3. [API 接口体系](#三api-接口体系)
4. [数据库设计](#四数据库设计)
5. [配置管理](#五配置管理)
6. [技术栈详解](#六技术栈详解)
7. [部署方式](#七部署方式)
8. [核心设计理念](#八核心设计理念)
9. [潜在优化方向](#九潜在优化方向)
10. [总结](#十总结)

---

## 一、项目概述

### 1.1 基本信息

- **项目名称**: 股票智能分析系统 (Daily Stock Analysis)
- **核心定位**: 基于 AI 大模型的 A股/港股/美股自选股智能分析平台
- **技术栈**: FastAPI + React + SQLite + LiteLLM
- **核心价值**: 每日自动分析并推送「决策仪表盘」到多渠道通知平台

### 1.2 系统架构

```
┌─────────────────────────────────────────────┐
│           前端层 (Frontend)                  │
│  ┌──────────┐    ┌──────────────────┐      │
│  │ Web UI   │    │ Electron Desktop │      │
│  │ (React)  │    │   (dsa-desktop)  │      │
│  └──────────┘    └──────────────────┘      │
└──────────────┬──────────────────────────────┘
               │ HTTP/WebSocket
┌──────────────▼──────────────────────────────┐
│         API 服务层 (FastAPI)                 │
│  ┌────────────────────────────────────┐     │
│  │  /api/v1/* RESTful 路由            │     │
│  │  - analysis (分析触发)             │     │
│  │  - stocks (行情查询)               │     │
│  │  - portfolio (持仓管理)            │     │
│  │  - agent (AI 对话)                 │     │
│  │  - history (历史记录)              │     │
│  └────────────────────────────────────┘     │
└──────────────┬──────────────────────────────┘
               │ 业务逻辑调用
┌──────────────▼──────────────────────────────┐
│       核心业务层 (Core Business)             │
│  ┌────────────────────────────────────┐     │
│  │ StockAnalysisPipeline              │     │
│  │ - 数据获取 → 技术分析 → LLM 分析   │     │
│  │ - 报告生成 → 通知推送              │     │
│  └────────────────────────────────────┘     │
└──────────────┬──────────────────────────────┘
               │ 数据访问
┌──────────────▼──────────────────────────────┐
│        数据层 (Data Layer)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ SQLite   │  │ Data     │  │ Search   │  │
│  │ Database │  │ Provider │  │ Service  │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
```

---

## 二、核心功能模块

### 2.1 智能分析流水线 (`StockAnalysisPipeline`)

**文件位置**: `src/core/pipeline.py`

#### 核心流程

1. **数据获取阶段** (断点续传支持)
   - 通过 `DataFetcherManager` 统一获取日线数据
   - 支持 AkShare/Tushare/YFinance/Longbridge 等多数据源
   - 实时行情与筹码分布采集 (带熔断保护)

2. **技术分析阶段**
   - 均线系统计算 (MA5/MA10/MA20/MA60)
   - 趋势状态识别 (多头/空头/震荡)
   - 量价关系分析 (量比/换手率/乖离率)

3. **基本面聚合阶段** (可选)
   - 财务指标提取 (PE/PB/营收/净利润)
   - 所属板块识别
   - 超时降级机制 (默认 8 秒)

4. **新闻情报检索**
   - 多维度搜索: 最新消息/风险排查/业绩预期/行业分析
   - 支持 SerpAPI/Tavily/Bocha/Brave/SearXNG 等搜索引擎
   - 社交舆情集成 (Reddit/X/Polymarket, 仅美股)

5. **LLM 分析阶段**
   - **传统模式**: 直接调用 LLM 生成结构化报告
   - **Agent 模式**: 基于 Skill 的策略问股 (15+ 内置策略)
   - 支持 Gemini/OpenAI/Claude/DeepSeek 等模型

6. **报告生成与持久化**
   - 生成 Markdown/HTML/PDF/DOCX 格式报告
   - 保存至 `AnalysisHistory` 表
   - 可选飞书云文档同步

7. **通知推送**
   - 单股即时推送或汇总推送
   - 支持企业微信/飞书/Telegram/Discord/Slack/Email
   - Markdown 转图片优化

### 2.2 Agent 策略问股系统

**文件位置**: `api/v1/endpoints/agent.py`

#### 核心能力

- **15+ 内置策略**:
  - 均线金叉/死叉分析
  - 缠论结构识别
  - 波浪理论判断
  - 多头趋势跟踪
  - 热点题材挖掘
  - 事件驱动分析
  - 成长质量评估
  - 预期重估策略

- **工具集** (Tool Registry):
  ```python
  TOOL_DISPLAY_NAMES = {
      "get_realtime_quote": "获取实时行情",
      "get_daily_history": "获取历史K线",
      "get_chip_distribution": "分析筹码分布",
      "search_stock_news": "搜索股票新闻",
      "analyze_trend": "分析技术趋势",
      "calculate_ma": "计算均线系统",
      # ... 更多工具
  }
  ```

- **多轮对话支持**:
  - 会话持久化 (`ConversationMessage` 表)
  - 自定义标题管理
  - SSE 流式响应

### 2.3 模拟交易与持仓管理

**文件位置**: `src/storage.py`

#### 核心功能

- **账户体系**: 
  - 真实账户/模拟账户分离
  - 多市场支持 (A股/港股/美股)
  - 多币种结算 (CNY/HKD/USD)

- **交易引擎**:
  - FIFO 成本核算
  - 分红除权处理
  - 汇率换算 (FX Rate Cache)
  - 实时盈亏计算

- **风险控制**:
  - 持仓集中度预警
  - 价格监盘后台任务 (每 5 分钟检查)
  - 自定义提醒规则 (Alert Rules)

### 2.4 回测系统

**文件位置**: `src/services/backtest_service.py`

#### 回测维度

- 方向正确率 (Direction Accuracy)
- 胜率统计 (Win Rate)
- 止盈止损命中率
- 模拟收益率 vs 实际收益率
- 平均持仓天数

---

## 三、API 接口体系

**路由注册**: `api/v1/router.py`

### 3.1 分析模块 (`/api/v1/analysis`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/analyze` | 触发股票分析 | 支持单股/批量分析 |
| GET | `/status/{task_id}` | 查询任务状态 | 返回进度百分比 |
| GET | `/tasks` | 获取任务列表 | 分页查询 |
| GET | `/tasks/stream` | SSE 实时推送 | WebSocket 替代方案 |
| POST | `/market-review` | 大盘复盘 | 独立于个股分析 |

**关键特性**:
- **防重复提交**: 相同股票代码正在分析时返回 409
- **异步任务队列**: 不阻塞请求,立即返回 `task_id`
- **SSE 实时推送**: 前端可订阅任务进度变化

### 3.2 股票模块 (`/api/v1/stocks`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/quote/{code}` | 获取实时行情 | 缓存 60 秒 |
| GET | `/history/{code}` | 获取历史 K 线 | 支持日期范围 |
| POST | `/import/image` | 图片导入自选股 | OCR 识别 |
| POST | `/import/csv` | CSV/Excel 导入 | 支持批量导入 |
| GET | `/autocomplete` | 股票代码补全 | 支持拼音/名称模糊搜索 |

### 3.3 持仓模块 (`/api/v1/portfolio`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/accounts` | 获取账户列表 | 支持筛选真实/模拟账户 |
| POST | `/accounts` | 创建新账户 | 指定市场/币种 |
| GET | `/trades` | 获取交易记录 | 分页+过滤 |
| POST | `/trades` | 录入交易 | 支持批量导入 |
| GET | `/positions` | 获取当前持仓 | 实时盈亏计算 |
| GET | `/snapshots` | 获取每日快照 | 权益曲线数据 |
| POST | `/import/excel` | Excel 导入持仓 | 解析券商导出文件 |

### 3.4 Agent 模块 (`/api/v1/agent`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/chat` | AI 对话 | 支持技能选择 |
| GET | `/skills` | 获取可用策略 | 返回策略列表 |
| GET | `/sessions` | 获取会话历史 | 分页查询 |
| DELETE | `/sessions/{id}` | 删除会话 | 级联删除消息 |
| GET | `/models` | 获取模型配置 | 用于前端下拉选择 |
| POST | `/deep-research` | 深度研究 | 长文本分析 |

### 3.5 历史模块 (`/api/v1/history`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/reports` | 获取分析报告 | 支持按股票/日期过滤 |
| GET | `/reports/{id}` | 获取报告详情 | 包含完整 Markdown |
| DELETE | `/reports/{id}` | 删除报告 | 级联删除回测结果 |
| GET | `/news-intel` | 获取新闻情报 | 多维度搜索结果 |
| GET | `/backtest-results` | 获取回测结果 | 按股票/日期查询 |

### 3.6 提醒模块 (`/api/v1/alerts`)

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/rules` | 获取提醒规则 | 支持类型过滤 |
| POST | `/rules` | 创建提醒规则 | 价格/涨跌幅/技术指标 |
| PUT | `/rules/{id}` | 更新规则 | 启用/禁用 |
| DELETE | `/rules/{id}` | 删除规则 | - |
| GET | `/triggers` | 获取触发历史 | 分页查询 |

---

## 四、数据库设计

**ORM 框架**: SQLAlchemy  
**数据库**: SQLite (支持 WAL 模式)  
**连接池**: 单例模式 (`DatabaseManager`)

### 4.1 核心数据表

#### 4.1.1 行情数据表

**`stock_daily`** - 股票日线数据

```sql
CREATE TABLE stock_daily (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code VARCHAR(10) NOT NULL,
    date DATE NOT NULL,
    open FLOAT,
    high FLOAT,
    low FLOAT,
    close FLOAT,
    volume FLOAT,
    amount FLOAT,
    pct_chg FLOAT,
    ma5 FLOAT,
    ma10 FLOAT,
    ma20 FLOAT,
    volume_ratio FLOAT,
    data_source VARCHAR(50),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(code, date)
);

CREATE INDEX ix_code_date ON stock_daily(code, date);
```

**字段说明**:
- `code`: 股票代码 (如 600519, hk00700, AAPL)
- `date`: 交易日期
- `open/high/low/close`: OHLC 数据
- `volume`: 成交量 (股)
- `amount`: 成交额 (元)
- `pct_chg`: 涨跌幅 (%)
- `ma5/ma10/ma20`: 移动平均线
- `volume_ratio`: 量比
- `data_source`: 数据来源标识

#### 4.1.2 分析历史表

**`analysis_history`** - 分析报告历史

```sql
CREATE TABLE analysis_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    query_id VARCHAR(64),
    code VARCHAR(10) NOT NULL,
    name VARCHAR(50),
    report_type VARCHAR(16),
    sentiment_score INTEGER,
    operation_advice VARCHAR(20),
    trend_prediction VARCHAR(50),
    analysis_summary TEXT,
    raw_result TEXT,
    news_content TEXT,
    context_snapshot TEXT,
    ideal_buy FLOAT,
    secondary_buy FLOAT,
    stop_loss FLOAT,
    take_profit FLOAT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX ix_analysis_code_time ON analysis_history(code, created_at);
CREATE INDEX idx_query_id ON analysis_history(query_id);
```

**字段说明**:
- `query_id`: 查询链路唯一标识 (UUID)
- `sentiment_score`: 综合评分 (0-100)
- `operation_advice`: 操作建议 (买入/观望/卖出)
- `trend_prediction`: 趋势预测 (看涨/看跌/震荡)
- `raw_result`: LLM 原始输出 JSON
- `context_snapshot`: 分析时的完整上下文快照
- `ideal_buy/secondary_buy`: 理想买点/次级买点
- `stop_loss/take_profit`: 止损位/止盈位

#### 4.1.3 新闻情报表

**`news_intel`** - 新闻搜索结果

```sql
CREATE TABLE news_intel (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    query_id VARCHAR(64),
    code VARCHAR(10) NOT NULL,
    name VARCHAR(50),
    dimension VARCHAR(32),
    query VARCHAR(255),
    provider VARCHAR(32),
    title VARCHAR(300) NOT NULL,
    snippet TEXT,
    url VARCHAR(1000) NOT NULL UNIQUE,
    source VARCHAR(100),
    published_date DATETIME,
    fetched_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    query_source VARCHAR(32),
    requester_platform VARCHAR(20),
    requester_user_id VARCHAR(64),
    requester_user_name VARCHAR(64),
    requester_chat_id VARCHAR(64),
    requester_message_id VARCHAR(64),
    requester_query VARCHAR(255)
);

CREATE INDEX ix_news_code_pub ON news_intel(code, published_date);
CREATE INDEX idx_dimension ON news_intel(dimension);
```

**字段说明**:
- `dimension`: 搜索维度 (latest_news/risk_check/earnings/market_analysis/industry)
- `provider`: 搜索引擎提供商 (serpapi/tavily/bocha/brave)
- `published_date`: 新闻发布时间
- `requester_*`: 请求者信息 (用于追踪来源)

#### 4.1.4 持仓相关表

**`portfolio_accounts`** - 账户信息

```sql
CREATE TABLE portfolio_accounts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    owner_id VARCHAR(64),
    name VARCHAR(64) NOT NULL,
    broker VARCHAR(64),
    market VARCHAR(8) NOT NULL DEFAULT 'cn',
    base_currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    account_type VARCHAR(16) NOT NULL DEFAULT 'real',
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX ix_portfolio_account_owner_active ON portfolio_accounts(owner_id, is_active);
CREATE INDEX idx_market ON portfolio_accounts(market);
```

**字段说明**:
- `owner_id`: 用户标识 (预留多用户扩展)
- `market`: 市场类型 (cn/hk/us)
- `base_currency`: 基础货币 (CNY/HKD/USD)
- `account_type`: 账户类型 (real/simulation)

**`portfolio_trades`** - 交易记录

```sql
CREATE TABLE portfolio_trades (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL REFERENCES portfolio_accounts(id),
    trade_uid VARCHAR(128),
    symbol VARCHAR(16) NOT NULL,
    market VARCHAR(8) NOT NULL DEFAULT 'cn',
    currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    trade_date DATE NOT NULL,
    side VARCHAR(8) NOT NULL,
    quantity FLOAT NOT NULL,
    price FLOAT NOT NULL,
    fee FLOAT DEFAULT 0.0,
    tax FLOAT DEFAULT 0.0,
    note VARCHAR(255),
    dedup_hash VARCHAR(64),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(account_id, trade_uid),
    UNIQUE(account_id, dedup_hash)
);

CREATE INDEX ix_portfolio_trade_account_date ON portfolio_trades(account_id, trade_date);
CREATE INDEX idx_symbol ON portfolio_trades(symbol);
```

**字段说明**:
- `side`: 交易方向 (buy/sell)
- `dedup_hash`: 去重哈希 (防止重复导入)
- `fee/tax`: 手续费和税费

**`portfolio_positions`** - 当前持仓

```sql
CREATE TABLE portfolio_positions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL REFERENCES portfolio_accounts(id),
    cost_method VARCHAR(8) NOT NULL DEFAULT 'fifo',
    symbol VARCHAR(16) NOT NULL,
    market VARCHAR(8) NOT NULL DEFAULT 'cn',
    currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    quantity FLOAT NOT NULL DEFAULT 0.0,
    avg_cost FLOAT NOT NULL DEFAULT 0.0,
    total_cost FLOAT NOT NULL DEFAULT 0.0,
    last_price FLOAT NOT NULL DEFAULT 0.0,
    market_value_base FLOAT NOT NULL DEFAULT 0.0,
    unrealized_pnl_base FLOAT NOT NULL DEFAULT 0.0,
    valuation_currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(account_id, symbol, market, currency, cost_method)
);

CREATE INDEX idx_symbol ON portfolio_positions(symbol);
```

**字段说明**:
- `cost_method`: 成本核算方法 (fifo/avg)
- `avg_cost`: 平均持仓成本
- `total_cost`: 总持仓成本
- `market_value_base`: 市值 (转换为基础货币)
- `unrealized_pnl_base`: 浮动盈亏 (本币)

**`portfolio_position_lots`** - FIFO 批次明细

```sql
CREATE TABLE portfolio_position_lots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL REFERENCES portfolio_accounts(id),
    cost_method VARCHAR(8) NOT NULL DEFAULT 'fifo',
    symbol VARCHAR(16) NOT NULL,
    market VARCHAR(8) NOT NULL DEFAULT 'cn',
    currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    open_date DATE NOT NULL,
    remaining_quantity FLOAT NOT NULL DEFAULT 0.0,
    unit_cost FLOAT NOT NULL DEFAULT 0.0,
    source_trade_id INTEGER REFERENCES portfolio_trades(id),
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX ix_portfolio_lot_account_symbol ON portfolio_position_lots(account_id, symbol);
```

**字段说明**:
- `open_date`: 开仓日期 (用于 FIFO 排序)
- `remaining_quantity`: 剩余数量 (卖出时递减)
- `unit_cost`: 该批次单位成本
- `source_trade_id`: 关联的买入交易 ID

**`portfolio_daily_snapshots`** - 每日权益快照

```sql
CREATE TABLE portfolio_daily_snapshots (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id INTEGER NOT NULL REFERENCES portfolio_accounts(id),
    snapshot_date DATE NOT NULL,
    cost_method VARCHAR(8) NOT NULL DEFAULT 'fifo',
    base_currency VARCHAR(8) NOT NULL DEFAULT 'CNY',
    total_cash FLOAT NOT NULL DEFAULT 0.0,
    total_market_value FLOAT NOT NULL DEFAULT 0.0,
    total_equity FLOAT NOT NULL DEFAULT 0.0,
    unrealized_pnl FLOAT NOT NULL DEFAULT 0.0,
    realized_pnl FLOAT NOT NULL DEFAULT 0.0,
    fee_total FLOAT NOT NULL DEFAULT 0.0,
    tax_total FLOAT NOT NULL DEFAULT 0.0,
    fx_stale BOOLEAN NOT NULL DEFAULT FALSE,
    payload TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(account_id, snapshot_date, cost_method)
);

CREATE INDEX idx_snapshot_date ON portfolio_daily_snapshots(snapshot_date);
```

**字段说明**:
- `total_cash`: 现金余额
- `total_market_value`: 总市值
- `total_equity`: 总权益 (现金 + 市值)
- `unrealized_pnl`: 浮动盈亏
- `realized_pnl`: 已实现盈亏
- `payload`: 完整持仓明细 JSON
- `fx_stale`: 汇率是否过期

#### 4.1.5 Agent 对话表

**`conversation_messages`** - 对话历史

```sql
CREATE TABLE conversation_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id VARCHAR(100) NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_session_id ON conversation_messages(session_id);
CREATE INDEX idx_created_at ON conversation_messages(created_at);
```

**字段说明**:
- `session_id`: 会话唯一标识
- `role`: 角色 (user/assistant/system)
- `content`: 消息内容 (支持 Markdown)

**`conversation_session_meta`** - 会话元数据

```sql
CREATE TABLE conversation_session_meta (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id VARCHAR(100) NOT NULL UNIQUE,
    custom_title VARCHAR(200),
    auto_title VARCHAR(200),
    sort_order INTEGER,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX idx_session_id_unique ON conversation_session_meta(session_id);
```

**字段说明**:
- `custom_title`: 用户自定义标题
- `auto_title`: 系统自动生成的标题 (基于首条消息)
- `sort_order`: 排序权重 (越小越靠前)

#### 4.1.6 回测结果表

**`backtest_results`** - 单条回测记录

```sql
CREATE TABLE backtest_results (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    analysis_history_id INTEGER NOT NULL REFERENCES analysis_history(id),
    code VARCHAR(10) NOT NULL,
    analysis_date DATE,
    eval_window_days INTEGER NOT NULL DEFAULT 10,
    engine_version VARCHAR(16) NOT NULL DEFAULT 'v1',
    eval_status VARCHAR(16) NOT NULL DEFAULT 'pending',
    evaluated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    operation_advice VARCHAR(20),
    position_recommendation VARCHAR(8),
    start_price FLOAT,
    end_close FLOAT,
    max_high FLOAT,
    min_low FLOAT,
    stock_return_pct FLOAT,
    direction_expected VARCHAR(16),
    direction_correct BOOLEAN,
    outcome VARCHAR(16),
    stop_loss FLOAT,
    take_profit FLOAT,
    hit_stop_loss BOOLEAN,
    hit_take_profit BOOLEAN,
    first_hit VARCHAR(16),
    first_hit_date DATE,
    first_hit_trading_days INTEGER,
    simulated_entry_price FLOAT,
    simulated_exit_price FLOAT,
    simulated_exit_reason VARCHAR(24),
    simulated_return_pct FLOAT,
    UNIQUE(analysis_history_id, eval_window_days, engine_version)
);

CREATE INDEX ix_backtest_code_date ON backtest_results(code, analysis_date);
CREATE INDEX idx_evaluated_at ON backtest_results(evaluated_at);
```

**字段说明**:
- `eval_window_days`: 评估窗口 (交易日数)
- `direction_expected`: 预期方向 (up/down/flat/not_down)
- `direction_correct`: 方向是否正确
- `outcome`: 结果 (win/loss/neutral)
- `first_hit`: 首先触发的目标 (take_profit/stop_loss/neither)
- `simulated_return_pct`: 模拟执行收益率

**`backtest_summaries`** - 回测汇总

```sql
CREATE TABLE backtest_summaries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    scope VARCHAR(16) NOT NULL,
    code VARCHAR(16),
    eval_window_days INTEGER NOT NULL DEFAULT 10,
    engine_version VARCHAR(16) NOT NULL DEFAULT 'v1',
    computed_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    total_evaluations INTEGER DEFAULT 0,
    completed_count INTEGER DEFAULT 0,
    insufficient_count INTEGER DEFAULT 0,
    long_count INTEGER DEFAULT 0,
    cash_count INTEGER DEFAULT 0,
    win_count INTEGER DEFAULT 0,
    loss_count INTEGER DEFAULT 0,
    neutral_count INTEGER DEFAULT 0,
    direction_accuracy_pct FLOAT,
    win_rate_pct FLOAT,
    neutral_rate_pct FLOAT,
    avg_stock_return_pct FLOAT,
    avg_simulated_return_pct FLOAT,
    stop_loss_trigger_rate FLOAT,
    take_profit_trigger_rate FLOAT,
    ambiguous_rate FLOAT,
    avg_days_to_first_hit FLOAT,
    advice_breakdown_json TEXT,
    diagnostics_json TEXT,
    UNIQUE(scope, code, eval_window_days, engine_version)
);

CREATE INDEX idx_scope ON backtest_summaries(scope);
```

**字段说明**:
- `scope`: 统计范围 (overall/stock)
- `direction_accuracy_pct`: 方向准确率 (%)
- `win_rate_pct`: 胜率 (%)
- `avg_simulated_return_pct`: 平均模拟收益率 (%)
- `advice_breakdown_json`: 建议分布统计 JSON
- `diagnostics_json`: 诊断信息 JSON

#### 4.1.7 提醒规则表

**`alert_rules`** - 提醒规则

```sql
CREATE TABLE alert_rules (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(64) NOT NULL,
    target_scope VARCHAR(32) NOT NULL DEFAULT 'single_symbol',
    target VARCHAR(64) NOT NULL,
    alert_type VARCHAR(32) NOT NULL,
    parameters TEXT NOT NULL DEFAULT '{}',
    severity VARCHAR(16) NOT NULL DEFAULT 'warning',
    enabled BOOLEAN NOT NULL DEFAULT TRUE,
    source VARCHAR(16) NOT NULL DEFAULT 'api',
    cooldown_policy TEXT,
    notification_policy TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX ix_alert_rule_type_target ON alert_rules(alert_type, target);
CREATE INDEX idx_enabled ON alert_rules(enabled);
```

**字段说明**:
- `target_scope`: 目标范围 (single_symbol/all/watchlist)
- `target`: 目标股票代码或通配符 (*)
- `alert_type`: 提醒类型 (price_change/volume_spike/ma_cross/rsi_oversold)
- `parameters`: 规则参数 JSON (如阈值、周期等)
- `severity`: 严重程度 (info/warning/critical)
- `cooldown_policy`: 冷却策略 JSON (避免频繁推送)

**`alert_triggers`** - 触发历史

```sql
CREATE TABLE alert_triggers (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id INTEGER,
    target VARCHAR(64) NOT NULL,
    observed_value FLOAT,
    threshold FLOAT,
    reason TEXT,
    data_source VARCHAR(64),
    data_timestamp DATETIME,
    triggered_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    status VARCHAR(16) NOT NULL DEFAULT 'triggered'
);

CREATE INDEX ix_alert_trigger_rule_time ON alert_triggers(rule_id, triggered_at);
CREATE INDEX idx_triggered_at ON alert_triggers(triggered_at);
```

**字段说明**:
- `observed_value`: 观测值 (如当前价格)
- `threshold`: 触发阈值
- `reason`: 触发原因描述
- `status`: 状态 (triggered/acknowledged/dismissed)

### 4.2 数据库特性

- **WAL 模式**: 支持并发读写,提升性能
- **Busy Timeout**: 默认 5000ms,自动重试写锁冲突
- **索引优化**: 所有外键和常用查询字段均建立索引
- **唯一约束**: 防止重复数据 (如 `code + date`, `url`)
- **自动迁移**: 启动时检测 schema 变更并自动执行 ALTER TABLE

---

## 五、配置管理

**文件位置**: `src/config.py`

### 5.1 环境变量加载

```bash
# .env 文件示例
STOCK_LIST=600519,hk00700,AAPL,TSLA
GEMINI_API_KEY=your_api_key_here
WECHAT_WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx
SCHEDULE_TIME=18:00
MAX_WORKERS=3
```

### 5.2 核心配置项

#### AI 模型配置

```bash
# 主模型 (必选其一)
ANSPIRE_API_KEYS=sk-xxx              # Anspire (推荐)
AIHUBMIX_KEY=sk-xxx                  # AIHubMix
GEMINI_API_KEY=AIzaSy...             # Google Gemini
OPENAI_API_KEY=sk-xxx                # OpenAI 兼容
ANTHROPIC_API_KEY=sk-ant-...         # Claude

# 多模型渠道 (可选)
LLM_CHANNELS=[
    {"provider": "gemini", "model": "gemini-2.5-pro"},
    {"provider": "openai", "model": "gpt-4o", "base_url": "..."}
]
```

#### 数据源配置

```bash
# 行情数据源优先级
REALTIME_SOURCE_PRIORITY=tickflow,akshare,longbridge,yfinance

# 搜索服务
SERPAPI_API_KEYS=xxx
TAVILY_API_KEYS=tvly-xxx
BOCHA_API_KEYS=bk-xxx
SEARXNG_BASE_URLS=http://localhost:8080
```

#### 通知渠道

```bash
# 至少配置一个
WECHAT_WEBHOOK_URL=...               # 企业微信
FEISHU_WEBHOOK_URL=...               # 飞书
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHAT_ID=...
DISCORD_WEBHOOK_URL=...
SLACK_BOT_TOKEN=...
SLACK_CHANNEL_ID=...
EMAIL_SENDER=xxx@gmail.com
EMAIL_PASSWORD=app_password
```

#### 高级配置

```bash
# 定时任务
SCHEDULE_ENABLED=true
SCHEDULE_TIME=18:00
SCHEDULE_RUN_IMMEDIATELY=true

# Agent 模式
AGENT_MODE=false                     # 显式启用
AGENT_SKILLS=all                     # 或指定策略列表
AGENT_MAX_STEPS=10

# 交易日检查
TRADING_DAY_CHECK_ENABLED=true
MARKET_REVIEW_REGION=cn              # cn/hk/us/all

# 性能调优
MAX_WORKERS=3
FUNDAMENTAL_STAGE_TIMEOUT_SECONDS=8.0
NEWS_MAX_AGE_DAYS=3
```

---

## 六、技术栈详解

### 6.1 后端技术栈

| 类别 | 技术选型 | 版本要求 |
|------|----------|----------|
| Web 框架 | FastAPI | >=0.100.0 |
| ASGI 服务器 | Uvicorn | >=0.23.0 |
| ORM | SQLAlchemy | >=2.0.0 |
| 数据库 | SQLite | 内置 |
| 数据验证 | Pydantic | >=2.0.0 |
| LLM 网关 | LiteLLM | >=1.0.0 |
| 定时任务 | APScheduler | >=3.10.0 |
| 日志系统 | Python logging | 内置 |
| 环境变量 | python-dotenv | >=1.0.0 |

### 6.2 前端技术栈

**Web 前端** (`apps/dsa-web/`):
- React 18 + TypeScript
- Ant Design 5.x (UI 组件库)
- React Query (数据请求)
- Zustand (状态管理)
- Vite (构建工具)

**桌面端** (`apps/dsa-desktop/`):
- Electron 28.x
- React (复用 Web 前端代码)
- Electron Builder (打包工具)

### 6.3 数据源适配器

**行情数据**:
- [AkShare](https://github.com/akfamily/akshare) - A股/港股免费数据
- [Tushare](https://tushare.pro/) - 专业金融数据 (需积分)
- [YFinance](https://github.com/ranaroussi/yfinance) - Yahoo Finance 美股数据
- [Longbridge](https://longportapp.com/) - 长桥证券 API
- [TickFlow](https://tickflow.org/) - 实时行情 (付费)

**新闻搜索**:
- [SerpAPI](https://serpapi.com/) - 搜索引擎结果
- [Tavily](https://tavily.com/) - AI 优化搜索
- [Bocha](https://open.bocha.cn/) - 博查中文搜索
- [Brave Search](https://brave.com/search/api/) - 隐私优先搜索
- SearXNG - 自建元搜索引擎

### 6.4 AI 模型支持

**云端模型**:
- Google Gemini (2.5 Pro/Flash)
- OpenAI GPT-4o/GPT-4 Turbo
- Anthropic Claude 3.5/3 Opus
- DeepSeek V2/V3
- 通义千问 Qwen-Max

**本地模型**:
- Ollama (Llama 3/Mistral/Qwen)
- 支持 OpenAI 兼容接口

---

## 七、部署方式

### 7.1 GitHub Actions (推荐)

**优势**:
- 零成本,无需服务器
- 自动定时执行 (工作日 18:00)
- 自动更新代码

**配置步骤**:
1. Fork 仓库
2. 配置 Secrets (API Keys + 自选股列表)
3. 启用 Actions
4. 手动测试触发

### 7.2 Docker 部署

```bash
# 拉取镜像
docker pull zhulinsen/daily_stock_analysis:latest

# 运行容器
docker run -d \
  --name stock-analysis \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/.env:/app/.env \
  -p 8000:8000 \
  zhulinsen/daily_stock_analysis:latest
```

### 7.3 本地运行

```bash
# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
vim .env

# 运行分析
python main.py

# 启动 Web 服务
python main.py --serve-only

# 定时任务模式
python main.py --schedule
```

---

## 八、核心设计理念

### 8.1 稳定性优先

- **降级策略**: 单一数据源失败不影响整体流程
- **熔断保护**: 连续失败后自动暂停数据源
- **超时控制**: 各阶段设置合理超时 (如基本面 8 秒)
- **写锁重试**: SQLite 并发写入自动重试 (最多 3 次)

### 8.2 可扩展性

- **插件化数据源**: 新增数据源只需实现 `BaseFetcher` 接口
- **Skill 系统**: 新增分析策略只需添加 YAML 配置文件
- **通知渠道扩展**: 实现 `NotificationChannel` 接口即可接入新渠道

### 8.3 用户体验

- **智能补全**: 支持股票名称/拼音/别名自动补全代码
- **断点续传**: 中断后可从上次失败处继续
- **实时反馈**: SSE 推送任务进度,避免盲等
- **多格式导出**: Markdown/HTML/PDF/DOCX 任选

---

## 九、潜在优化方向

### 9.1 性能优化

1. **数据库分片**: 当 `stock_daily` 超过百万行时考虑按年份分表
2. **Redis 缓存**: 实时行情/热门搜索结果缓存
3. **异步任务**: 将耗时操作 (如 PDF 生成) 移至 Celery/RQ

### 9.2 功能增强

1. **多用户支持**: 当前为单用户设计,可扩展为多租户架构
2. **WebSocket 推送**: 替代 SSE,实现双向通信
3. **策略回测可视化**: 增加 K 线图标注买卖点

### 9.3 安全性

1. **API 认证**: 当前 Web UI 无强制登录,建议增加 JWT
2. **速率限制**: 防止 API 滥用 (如每分钟最多 10 次分析)
3. **敏感信息加密**: `.env` 文件中的 API Keys 应加密存储

---

## 十、总结

### 系统评价

您的股票智能分析系统是一个**架构清晰、功能完备、工程化程度高**的优秀项目:

✅ **优点**:
- 模块化设计,职责边界清晰
- 完善的错误处理和降级机制
- 丰富的数据源和通知渠道支持
- Agent 系统具备高度可扩展性
- 详细的文档和 CI/CD 自动化

⚠️ **注意事项**:
- 大规模使用时需关注 SQLite 性能瓶颈
- Agent 模式成本较高,建议设置预算护栏
- 部分第三方 API (如 SerpAPI) 有配额限制

🎯 **适用场景**:
- 个人投资者日常选股辅助
- 量化策略初步验证
- 学习 AI 在金融领域的应用

### 后续开发建议

如果您需要针对某个具体模块进行深入开发,可以考虑以下方向:

1. **新增数据源**: 实现新的 `BaseFetcher` 子类
2. **优化回测算法**: 增加更多评估指标和可视化
3. **扩展 Agent 策略**: 添加自定义 Skill YAML 文件
4. **性能调优**: 引入 Redis 缓存和异步任务队列
5. **安全加固**: 增加 JWT 认证和速率限制

如需详细的技术方案和代码实现,欢迎随时咨询!

---

**文档生成**: 2026-05-23  
**基于版本**: 最新代码审查  
**维护者**: AI Assistant
